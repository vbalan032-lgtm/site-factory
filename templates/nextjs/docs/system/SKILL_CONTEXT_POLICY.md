# Skill Context Policy

Query the graph first for compact discovery. Verify claims, approvals, changed fingerprints and release evidence in exact files. If the graph is absent, stale or insufficient, use only the stage `context_allowlist` fallback. Never load an entire skill family when one owner stage is enough.

