# Окружение Codex

Project skills находятся только в `.agents/skills`. Core MCP: Context7 и Playwright. GitHub и внешние private integrations подключаются отдельными профилями и используют только OS environment или secret store.

